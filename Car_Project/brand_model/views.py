from django.shortcuts import render
from .models import Brand

# Create your views here.

# def brand_list(request, category_slug = None):
    
#     brands = Brand.objects.all()
#     return render(request,'home.html',{'brands':brands})
